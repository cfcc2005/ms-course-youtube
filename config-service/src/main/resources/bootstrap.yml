server:
  port: 8081
  
spring:
  profiles:
    active: composite
  cloud:
    config:
      server:
        composite:
          - type: native
            search-locations: file:///D:/ProyectosPrueba/ProyectoRestCurso/config-data/
           
  security:
    user:
      name: root
      password: s3cr3t
